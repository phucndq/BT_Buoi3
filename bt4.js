let student = {
    name: "Nguyễn Đặng Quang Phúc",
    age: 18,
    averageScore: 7.9
  };
  
  function printStudentInfo(student) {
    console.log(`Tên: ${student.name}`);
    console.log(`Tuổi: ${student.age}`);
    console.log(`Điểm trung bình: ${student.averageScore}`);
  }
  
  printStudentInfo(student);